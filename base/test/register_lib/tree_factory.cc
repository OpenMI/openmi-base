#include "tree_factory.h"

//using namespace dmlc;

/*
DMLC_REGISTRY_ENABLE(TreeFactory);
DMLC_REGISTRY_LINK_TAG(binary_tree);
DMLC_REGISTRY_LINK_TAG(avl_tree);
*/ 

using namespace openmi;

OPENMI_REGISTER_ENABLE(TreeFactory);
OPENMI_REGISTER_LINK_TAG(binary_tree);
OPENMI_REGISTER_LINK_TAG(avl_tree);
